project(${CMAKE_PROJECT_NAME})

# inteface is needed, because every dependencies the headers have must be visible by target that are linked with the libraries

#add_library(
#    emulator 
#    INTERFACE 


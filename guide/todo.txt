- FIX load isntruction: it depends on id not more available. It also seems that not all possible load instructions were implemented
-Test basic instructions: store, load, add shift, ...
-Change CmakeSrc to avoid unnecessary source file recompilation
